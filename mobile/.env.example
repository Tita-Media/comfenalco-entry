# URL del backend desplegado en Vercel (sin slash final)
EXPO_PUBLIC_API_URL=https://tiquetera.vercel.app

# Supabase (mismo proyecto que web/): solo URL y anon key — nunca la service role
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
